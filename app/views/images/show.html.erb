<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

<div class="container">
  <div class="row">

    <h1>Image Details</h1>

    <div class="row">
      <div class="col-4">
        <table class="table-primary">
          <tr>
            <th>Name</th>
            <!-- Renders the "name" attribute of the image record -->
            <td><%= @image.name %></td>
          </tr>
          <tr>
            <th>Alt Text</th>
            <!-- Renders the "alt_text" attribute of the image record -->
            <td><%= @image.alt_text %></td>
          </tr>
          <tr>
            <th>Category</th>
            <!-- Renders the "category" attribute of the image record -->
            <td><%= @image.category %></td>
          </tr>
        </table>
      </div>
      <div class="col-4">
        <%= image_tag url_for(@image.file), :class => "img-thumbnail" %>
      </div>
    </div>


    <div class="btn-group">
      <!--
          <a  class="btn btn-primary"
              href="/images/new.{image-id}">Back to Gallery
          </a>
        -->
      <%= link_to "Back to Gallery", new_image_path(@image), :class => "btn btn-primary" %>

      <!--
          <a  class="btn btn-primary"
              href="/images/new.{image-id}">Back to Gallery
          </a>
        -->
      <%= link_to "Edit", edit_image_path(@image), :class => "btn btn-warning" %>

      <!--
          <a  data-turbo-method="delete"
              data-turbo-confirm="Are you sure?"
              class="btn btn-danger"
              href="/images/{image-id}">Remove image and details
          </a>
        -->
      <%= link_to "Remove image and details", image_path(@image), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
      }, :class => "btn btn-danger" %>
    </div>
  </div>
</div>